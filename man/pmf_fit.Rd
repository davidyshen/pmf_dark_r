% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmf_dark.R
\name{pmf_fit}
\alias{pmf_fit}
\title{Fit a PMFDark Model (Object-Oriented API)}
\usage{
pmf_fit(
  y,
  x,
  model_type = "gaussian",
  num_factors = 1,
  method = "svi",
  cuda = FALSE,
  categorical_cols = NULL,
  batch_size = NULL,
  model_args = list(),
  ...
)
}
\arguments{
\item{y}{Species presence-absence/count matrix (n_sites, n_species)}

\item{x}{Environmental predictor matrix (n_sites, n_env)}

\item{model_type}{Ecological response model: "linear", "gaussian", or "bnn" (default: "gaussian")}

\item{num_factors}{Number of latent factors for residual covariance (default: 1)}

\item{method}{Inference method: "svi" or "mcmc" (default: "svi")}

\item{cuda}{Use GPU computation (SVI only) (default: FALSE)}

\item{categorical_cols}{Explicit list of column names in x to treat as categorical variables (default: NULL)}

\item{batch_size}{Mini-batch size (SVI only) (default: NULL)}

\item{model_args}{List of extra response-model specific arguments passed to the `PMFDark` constructor.}

\item{...}{Extra hyperparameters for SVI (e.g. `num_iterations`, `lr`) or MCMC (e.g. `num_chains`) passed to `model$fit()`.}
}
\value{
A fitted `PMFDark` Python model object.
}
\description{
Instantiates a `PMFDark` model from the Python package `pmf_dark` and trains
it on the provided datasets.
}
